# Implementation Plan: t-3 (Optimize JSON unmarshaling for large datasets)

## Context
As the project grows, loading hundreds of tasks from individual JSON files may become a bottleneck.

## Technical Approach
- Profile current `ListTasks` performance.
- Implement a concurrent loading strategy using a worker pool.
- Consider caching metadata in a single `.index.json` file if necessary.

## Work Tree
- [ ] Benchmarking script
- [ ] Parallel file reading
- [ ] Optional indexing layer
